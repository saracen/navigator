#!/bin/bash
docker build --build-arg GIT_USERNAME=$GIT_USERNAME --build-arg GIT_REPOSITORY=$GIT_REPOSITORY --build-arg SOURCE_BRANCH=$SOURCE_BRANCH --build-arg GITHUB_TOKEN=$GITHUB_TOKEN -f $DOCKERFILE_PATH -t $IMAGE_NAME .